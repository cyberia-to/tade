[package]
name = "tape"
version = "0.1.0"
edition = "2024"
description = "TAPE — Typed Annotated Payload Exchange. A typed framed byte-stream protocol."
license = "Cyber"
repository = "https://github.com/cyberia-to/tape"

[dependencies]
bytes = "1"

[dev-dependencies]
